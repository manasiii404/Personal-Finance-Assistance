# ML Service — Required Dependencies Only
# Used in: main.py, transaction_categorizer.py, expense_forecaster.py, config.py

# API Framework
fastapi==0.109.2
uvicorn==0.27.1
pydantic==2.6.1
python-multipart==0.0.9

# ML — Transaction Categorizer (TF-IDF + Random Forest)
scikit-learn==1.4.0

# ML — Expense Forecaster (Prophet time-series)
prophet==1.1.5

# Data Processing
pandas==2.2.0
numpy==1.26.4
joblib==1.3.2

# Config / Env
python-dotenv==1.0.0
